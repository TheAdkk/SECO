[package]
name = "xtask"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
# Only for the CLAP ABI mirrors: xtask reads the built plugin's own
# descriptor instead of being told what the plugin is called.
seco-clap = { path = "../seco-clap" }
